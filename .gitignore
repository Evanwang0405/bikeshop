node_modules/
.next/
out/
.env*
.DS_Store
*.log

# TypeScript incremental build info
*.tsbuildinfo

# Scraped raw source caches (regenerate with scripts/ingest-*.mjs)
# Pattern-based so a new ingest-*.mjs script cannot leak its cache into the repo.
scripts/.*-raw.json
scripts/.*-generated.json
